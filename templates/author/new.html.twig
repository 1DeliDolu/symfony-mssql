{% extends 'base.html.twig' %}

{% block title %}New Author - Pubs Admin{% endblock %}

{% block body %}
    <div class="grid">
        <section class="panel">
            <div class="panel-head">
                <div>
                    <h2>Create author</h2>
                    <p class="meta">Add a new author to the catalog.</p>
                </div>
                <a class="btn btn-secondary" href="{{ path('author_index') }}">← Back to authors</a>
            </div>
            {{ include('author/_form.html.twig') }}
        </section>
    </div>
{% endblock %}
