<form method="post" action="/employees/{{ employee.empId }}/delete" onsubmit="return confirm('Delete this employee?');" class="stack">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee.empId) }}">
    <button class="btn btn-danger">Delete</button>
</form>
