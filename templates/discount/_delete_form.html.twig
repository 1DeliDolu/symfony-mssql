<form method="post" action="{{ path('discount_delete', {'discountType': discount.discountType}) }}" onsubmit="return confirm('Delete this discount?');" class="stack">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discount.discountType) }}">
    <button class="btn btn-danger">Delete</button>
</form>
