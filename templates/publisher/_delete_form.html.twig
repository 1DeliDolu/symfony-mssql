<form method="post" action="{{ path('publisher_delete', {'id': publisher.id}) }}" onsubmit="return confirm('Delete this publisher?');" class="stack">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publisher.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
