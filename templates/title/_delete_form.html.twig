<form method="post" action="/titles/{{ title.id }}/delete" onsubmit="return confirm('Are you sure you want to delete this title?');" class="stack">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ title.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
