<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Pubs Admin{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <style>
            :root {
                --bg: #0f172a;
                --panel: #1e293b;
                --panel-light: #273449;
                --text: #f8fafc;
                --muted: #94a3b8;
                --accent: #38bdf8;
                --accent-dark: #0ea5e9;
                --border: rgba(148,163,184,.2);
                --danger: #f87171;
                --success: #34d399;
            }
            * { box-sizing: border-box; }
            body {
                margin: 0;
                font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
                background: radial-gradient(circle at top, rgba(56,189,248,.12), transparent 55%), var(--bg);
                color: var(--text);
                min-height: 100vh;
                display: flex;
                justify-content: center;
                padding: 2rem;
            }
            a { color: var(--accent); text-decoration: none; }
            a:hover { color: var(--accent-dark); }
            .app-shell {
                width: min(1300px, 100%);
                background: rgba(15,23,42,.65);
                border: 1px solid rgba(148,163,184,.15);
                border-radius: 1.2rem;
                backdrop-filter: blur(16px);
                box-shadow: 0 15px 60px rgba(15,23,42,.45);
                overflow: hidden;
            }
            header {
                padding: 1.5rem 2rem;
                border-bottom: 1px solid rgba(148,163,184,.2);
                display: flex;
                align-items: center;
                justify-content: space-between;
                background: linear-gradient(120deg, rgba(56,189,248,.2), rgba(15,23,42,.4));
            }
            header h1 {
                margin: 0;
                font-size: 1.2rem;
                letter-spacing: .08em;
                text-transform: uppercase;
                color: var(--muted);
            }
            .main-nav {
                display: flex;
                gap: .75rem;
                align-items: center;
                position: relative;
            }
            .main-nav > a {
                padding: .35rem .9rem;
                border-radius: .65rem;
                font-size: .9rem;
                border: 1px solid transparent;
                color: var(--text);
                transition: border-color .15s ease, background .15s ease;
            }
            .main-nav > a:hover {
                border-color: rgba(148,163,184,.4);
                background: rgba(148,163,184,.1);
            }
            .nav-group {
                position: relative;
            }
            .nav-trigger {
                display: inline-flex;
                align-items: center;
                gap: .35rem;
                padding: .35rem .9rem;
                border-radius: .65rem;
                border: 1px solid transparent;
                color: var(--text);
                font-size: .9rem;
                cursor: pointer;
                transition: border-color .15s ease, background .15s ease;
            }
            .nav-group:hover .nav-trigger {
                border-color: rgba(148,163,184,.4);
                background: rgba(148,163,184,.1);
            }
            .nav-trigger svg {
                width: 0.75rem;
                height: 0.75rem;
            }
            .nav-dropdown {
                position: absolute;
                top: calc(100% + .4rem);
                left: 0;
                background: rgba(15,23,42,.95);
                border: 1px solid rgba(148,163,184,.25);
                border-radius: .85rem;
                padding: .6rem;
                display: flex;
                flex-direction: column;
                gap: .3rem;
                min-width: 12rem;
                box-shadow: 0 10px 30px rgba(15,23,42,.45);
                transition: opacity .15s ease, transform .15s ease;
                z-index: 10;
                opacity: 0;
                visibility: hidden;
                transform: translateY(-4px);
            }
            .nav-group:hover .nav-dropdown,
            .nav-group.open .nav-dropdown {
                opacity: 1;
                visibility: visible;
                transform: translateY(0);
            }
            .nav-dropdown a {
                padding: .45rem .6rem;
                border-radius: .55rem;
                color: var(--text);
                font-size: .9rem;
                border: 1px solid transparent;
            }
            .nav-dropdown a:hover {
                border-color: rgba(56,189,248,.4);
                background: rgba(56,189,248,.12);
            }
            main {
                padding: 2rem;
            }
            .grid {
                display: grid;
                gap: 1.5rem;
            }
            .panel {
                background: var(--panel);
                border: 1px solid var(--border);
                border-radius: 1rem;
                padding: 1.5rem;
                box-shadow: inset 0 1px 0 rgba(255,255,255,.03);
            }
            .panel-link {
                text-decoration: none;
                color: inherit;
                transition: transform .15s ease, border-color .15s ease;
                display: block;
            }
            .panel-link:hover {
                transform: translateY(-4px);
                border-color: rgba(56,189,248,.5);
            }
            .panel-head {
                display: flex;
                justify-content: space-between;
                align-items: center;
                gap: 1rem;
                margin-bottom: 1.25rem;
            }
            .panel-head h2 {
                margin: 0;
                font-size: 1.3rem;
            }
            .meta {
                color: var(--muted);
                font-size: .9rem;
            }
            table {
                width: 100%;
                border-collapse: collapse;
            }
            .table-wrapper {
                overflow-x: auto;
            }
            th, td {
                padding: .85rem 1rem;
                text-align: left;
            }
            th {
                font-size: .85rem;
                letter-spacing: .05em;
                text-transform: uppercase;
                color: var(--muted);
            }
            tbody tr {
                border-top: 1px solid var(--border);
            }
            tbody tr:hover {
                background: rgba(148,163,184,.08);
            }
            .btn {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                gap: .4rem;
                padding: .55rem 1.2rem;
                border-radius: .75rem;
                border: none;
                cursor: pointer;
                font-weight: 600;
                color: var(--text);
                background: var(--accent);
            }
            .btn:hover { background: var(--accent-dark); }
            .btn-secondary {
                background: rgba(148,163,184,.15);
                color: var(--text);
            }
            .btn-secondary:hover { background: rgba(148,163,184,.25); }
            .btn-danger {
                background: var(--danger);
            }
            .btn-danger:hover {
                background: #ef4444;
            }
            .stack {
                display: flex;
                gap: .6rem;
                flex-wrap: wrap;
            }
            .table-actions {
                display: inline-flex;
                gap: .75rem;
                font-size: .9rem;
            }
            .empty-state {
                padding: 2rem;
                text-align: center;
                color: var(--muted);
            }
            .quick-grid {
                display: grid;
                gap: 1rem;
                grid-template-columns: repeat(1, minmax(0, 1fr));
            }
            @media (min-width: 600px) {
                .quick-grid {
                    grid-template-columns: repeat(2, minmax(0, 1fr));
                }
            }
            @media (min-width: 900px) {
                .quick-grid {
                    grid-template-columns: repeat(3, minmax(0, 1fr));
                }
            }
            .form-actions {
                display: flex;
                gap: .75rem;
                flex-wrap: wrap;
            }
            form {
                display: grid;
                gap: 1rem;
            }
            form.stack {
                display: flex;
                align-items: center;
            }
            label { font-size: .9rem; color: var(--muted); }
            input, select {
                width: 100%;
                padding: .7rem .85rem;
                border-radius: .6rem;
                border: 1px solid var(--border);
                background: var(--panel-light);
                color: var(--text);
                font-size: 1rem;
            }
            input:focus, select:focus {
                outline: none;
                border-color: var(--accent);
                box-shadow: 0 0 0 2px rgba(56,189,248,.15);
            }
            .flash {
                padding: .6rem 1rem;
                border-radius: .7rem;
                margin-bottom: 1rem;
                font-size: .9rem;
            }
            .flash-success {
                background: rgba(52,211,153,.15);
                border: 1px solid rgba(52,211,153,.4);
                color: #a7f3d0;
            }
            .flash-error {
                background: rgba(248,113,113,.15);
                border: 1px solid rgba(248,113,113,.4);
                color: #fecaca;
            }
            @media (max-width: 700px) {
                body { padding: 1rem; }
                header { flex-direction: column; gap: 1rem; }
                nav { flex-wrap: wrap; justify-content: center; }
                main { padding: 1.5rem; }
                th, td { padding: .6rem .4rem; }
            }
        </style>
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </head>
    <body>
        <div class="app-shell">
            <header>
                <h1>Pubs Admin</h1>
                <nav class="main-nav">
                    <a href="{{ path('home_page') }}">Dashboard</a>
                    <div class="nav-group">
                        <span class="nav-trigger">
                            Catalog
                            <svg viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
                                <path d="M2 4.5L6 8.5L10 4.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
                            </svg>
                        </span>
                        <div class="nav-dropdown">
                            <a href="{{ path('author_index') }}">Authors</a>
                            <a href="{{ path('title_page_index') }}">Titles</a>
                            <a href="{{ path('title_author_page_index') }}">Title Authors</a>
                            <a href="{{ path('publisher_index') }}">Publishers</a>
                        </div>
                    </div>
                    <div class="nav-group">
                        <span class="nav-trigger">
                            Operations
                            <svg viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
                                <path d="M2 4.5L6 8.5L10 4.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
                            </svg>
                        </span>
                        <div class="nav-dropdown">
                            <a href="{{ path('store_page_index') }}">Stores</a>
                            <a href="{{ path('sale_page_index') }}">Sales</a>
                            <a href="{{ path('discount_index') }}">Discounts</a>
                            <a href="{{ path('roysched_page_index') }}">Roysched</a>
                        </div>
                    </div>
                    <div class="nav-group">
                        <span class="nav-trigger">
                            People
                            <svg viewBox="0 0 12 12" fill="none" xmlns="http://www.w3.org/2000/svg">
                                <path d="M2 4.5L6 8.5L10 4.5" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/>
                            </svg>
                        </span>
                        <div class="nav-dropdown">
                            <a href="{{ path('employee_page_index') }}">Employees</a>
                            <a href="{{ path('job_page_index') }}">Jobs</a>
                            <a href="{{ path('pub_info_page_index') }}">Pub Info</a>
                        </div>
                    </div>
                </nav>
            </header>
            <main>
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="flash flash-{{ label }}">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
                {% block body %}{% endblock %}
            </main>
        </div>
        <script>
            (function () {
                const groups = document.querySelectorAll('.nav-group');
                groups.forEach(group => {
                    let hideTimer;
                    group.addEventListener('mouseenter', () => {
                        clearTimeout(hideTimer);
                        group.classList.add('open');
                    });
                    group.addEventListener('mouseleave', () => {
                        hideTimer = setTimeout(() => group.classList.remove('open'), 200);
                    });
                });
            })();
        </script>
    </body>
</html>
