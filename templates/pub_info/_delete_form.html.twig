<form method="post" action="/pub-info/{{ pub_info.pubId }}/delete" onsubmit="return confirm('Delete this publisher info?');" class="stack">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pub_info.pubId) }}">
    <button class="btn btn-danger">Delete</button>
</form>
